pipeline{
    agent any
    tools{
        maven 'Maven'
    }
    stages{
        stage('Build & Sonarqube Analysis'){
            agent any
            steps{
                withSonarQubeEnv('sonar-server')
                {
                    sh 'mvn clean package sonar:sonar'
                }
            }
        }
    }
}
